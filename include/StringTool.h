#ifndef STRING_TOOL_H
#define STRING_TOOL_H
// Header contains util functions to deal with c-strings.


// Returns the length until a character ch appears in the string.
// If ch not found, returns -1.
int StringTool_len_to_ch(const char* string, char ch);

#endif
