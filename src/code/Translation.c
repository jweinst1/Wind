#include "Translation.h"

void Translate_src_code(const char* code, WindStream* ws)
{
        while(*code)
        {
                switch(*code)
                {
                default:
                        // need error handling
                }
        }
}
