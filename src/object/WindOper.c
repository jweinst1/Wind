#include "WindOper.h"

void WindOper_add(WindObject* obj1, WindObject* obj2)
{
        switch(obj1->type)
        {
        case WindInstruc_Int:
                switch(obj2->type)
                {
                default:
                        return;
                }
                return;
        }
}
